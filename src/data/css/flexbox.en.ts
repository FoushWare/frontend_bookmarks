export const flexboxContent = {
  title: 'Flexbox',
  icon: '📦',
  description: 'Master flexible box layout for one-dimensional layouts along a main axis and cross axis.',
  eyebrow: '🎛️ Flexbox Guide',
  heroTitle: 'Learn <span>Flexbox</span> by Playing With It, Not Memorizing',
  subtitle: `Imagine you have a box with a few items inside it, and you want to control where they sit, their order, and the spacing between them — that's where Flexbox comes in. All you have to do is turn the box into a <code>Flex Container</code> with one line: <code>display: flex</code>, and then you can easily control how the items inside it are arranged. Try it yourself below 👇`,
  sections: [
    {
      eyebrow: '🧩 Start here',
      title: "Who's the <code>Container</code>, and who are the <code>Items</code>?",
      description: `You have a parent element, with a few child elements inside it. When you write <code>display: flex</code> on the parent, its direct children automatically become "flex items" (<code>Flex Items</code>), and the parent itself becomes a "flex container" (<code>Flex Container</code>) that controls their layout. Simply put: <b>the parent = <code>Container</code></b>, <b>the direct children = <code>Items</code></b>.`,
      diagram: true,
    },
    {
      eyebrow: '🧭 The most important idea in Flexbox',
      title: 'There are two axes you need to understand',
      description: `Flexbox works with two axes: the <b><code>Main Axis</code></b>, which is the primary direction items flow along, and the <b><code>Cross Axis</code></b>, which runs perpendicular to the main axis. What decides the direction of the Main Axis? <code>flex-direction</code>. If it's <code>row</code>, items flow horizontally; if it's <code>column</code>, they flow top to bottom.`,
      axes: true,
      callout: {
        icon: '⚠️',
        title: 'Watch out with RTL:',
        content: `if a page is written right-to-left (like Arabic), the <code class="mono">row</code> direction starts from the <b>right</b> instead of the left. So <code class="mono">flex-start</code> isn't always on the left — a very common source of confusion, especially when switching between LTR and RTL codebases. Try it yourself in the lab below.`,
      },
    },
    {
      eyebrow: '🎮 Try it yourself',
      title: 'Move the items around and see what happens',
      description: 'Change each option and watch the boxes update live, along with the code changing below every time.',
      controls: {
        direction: {
          label: 'flex-direction — item direction',
          options: ['row', 'row-reverse', 'column', 'column-reverse'],
        },
        wrap: {
          label: 'flex-wrap — wrap to a new line or not?',
          options: ['nowrap', 'wrap'],
        },
        justify: {
          label: 'justify-content — how do they spread out?',
          options: ['flex-start', 'center', 'flex-end', 'space-between', 'space-around', 'space-evenly'],
        },
        align: {
          label: 'align-items — how do they align on the other axis?',
          options: ['stretch', 'flex-start', 'center', 'flex-end'],
        },
        alignSelf: {
          label: 'align-self — for the second item (yellow)',
          options: ['auto', 'flex-start', 'center', 'flex-end', 'stretch'],
        },
        flexFlow: {
          label: 'flex-flow — shorthand for direction and wrap',
          options: ['', 'row nowrap', 'row wrap', 'column nowrap', 'column wrap'],
        },
      },
    },
    {
      eyebrow: '⚡ flex-flow shorthand',
      title: '<code>flex-flow</code> — direction and wrap in one property',
      description: `<code>flex-flow</code> is a shorthand that combines <code>flex-direction</code> and <code>flex-wrap</code> into a single property. The syntax is <code>flex-flow: direction wrap;</code>. It's commonly used when you want to set both properties together for cleaner code.`,
      flexFlowNote: true,
    },
    {
      eyebrow: '📦 Who gets the extra space?',
      title: '<code>flex-grow</code>, <code>flex-shrink</code>, <code>flex-basis</code>',
      description: `Not all items need to stay the same size. These three properties work together in 3 steps: First, Flexbox determines each item's base size (<code>flex-basis</code>), then it checks if there's extra space or not — if there is, it distributes it according to <code>flex-grow</code>, and if there's not enough, it shrinks items according to <code>flex-shrink</code>. Try each one individually below to see the idea more clearly.`,
      growShrinkBasis: true,
    },
    {
      eyebrow: '🔀 Change order without touching HTML',
      title: '<code>order</code>',
      description: `All Flexbox items have a default <code>order</code> value of <code>0</code>. The browser arranges them visually according to this number from smallest to largest — not according to their order in the HTML. This means you can make the last item in code appear first on screen without touching the markup at all, which is very useful for responsive design (like changing sidebar column order on mobile without reordering HTML).`,
      orderLab: true,
      orderWarning: '⚠️ Accessibility Warning: <code>order</code> only changes visual order, leaving DOM order and Tab navigation order (Reading order / Tab order) unchanged. This means someone using a screen reader or keyboard might read or navigate in a completely different order than what they see visually on screen — use it carefully and avoid reordering important information with it.',
    },
    {
      eyebrow: '�� <code>justify-content</code>',
      title: 'Want to distribute items along the main axis?',
      description: "That's exactly what <code>justify-content</code> does. The difference between values really shows when there's leftover space inside the <code>Container</code> — try each one and see the difference for yourself.",
      gallery: [
        { value: 'flex-start', description: 'items start from the beginning of the axis' },
        { value: 'center', description: 'all items gather in the middle' },
        { value: 'flex-end', description: 'items go to the end of the axis' },
        { value: 'space-between', description: 'first item at the start, last item at the end, with equal gaps between them' },
        { value: 'space-around', description: 'every item gets space around it' },
        { value: 'space-evenly', description: 'the space between every item, and at the edges, is exactly equal' },
      ],
    },
    {
      eyebrow: '↕️ <code>align-items</code>',
      title: 'Now how do we control the other direction?',
      description: "That's where <code>align-items</code> comes in. If your <code>flex-direction</code> is <code>row</code>, the cross axis is vertical.",
      gallery: [
        { value: 'flex-start', description: 'items go to the start of the cross axis' },
        { value: 'center', description: 'items center themselves' },
        { value: 'flex-end', description: 'items go to the end of the cross axis' },
        { value: 'stretch', description: 'items stretch to fill the space; this is the default when an item\'s height isn\'t set' },
      ],
    },
  ],
  exercises: [
    {
      id: 1,
      instruction: 'You want the items stacked on top of each other instead of side by side. Complete the code:',
      expectedProp: 'flex-direction',
      expectedValue: 'column',
      success: 'Nice! ✅ column stacks the items on top of each other instead of side by side.',
      hint: 'Not quite. The value that stacks items vertically is column — try again.',
    },
    {
      id: 2,
      instruction: 'You want the items to gather in the middle of the box horizontally. Complete the code:',
      expectedProp: 'justify-content',
      expectedValue: 'center',
      success: 'Nice! ✅ center centers the items along the main axis.',
      hint: 'Not quite. The value that centers items horizontally is center — try again.',
    },
    {
      id: 3,
      instruction: 'You have items wider than the box, and you want them to wrap to a new line instead of scrolling. Complete the code:',
      expectedProp: 'flex-wrap',
      expectedValue: 'wrap',
      success: 'Nice! ✅ wrap makes items drop to a new line once the space runs out.',
      hint: 'Not quite. The value that makes items wrap to a new line is wrap — try again.',
    },
    {
      id: 4,
      instruction: 'You have items with different heights, and you want them all centered vertically together. Complete the code:',
      expectedProp: 'align-items',
      expectedValue: 'center',
      success: 'Nice! ✅ align-items: center centers the items vertically on the cross axis.',
      hint: 'Not quite. The value that centers items vertically is center — try again.',
    },
    {
      id: 5,
      instruction: 'You want the first item pinned to the start of the box, the last item at the end, and equal spacing between them. Complete the code:',
      expectedProp: 'justify-content',
      expectedValue: 'space-between',
      success: 'Nice! ✅ space-between pins the first item to the start and the last to the end, with equal spacing between them.',
      hint: 'Not quite. The value that distributes space only between items (with no space at the edges) is space-between — try again.',
    },
  ],
  cheatSheet: {
    container: [
      { property: 'display: flex', description: 'activates Flexbox' },
      { property: 'flex-direction', description: 'sets the direction of items' },
      { property: 'flex-wrap', description: 'controls whether items wrap to a new line' },
      { property: 'flex-flow', description: 'shorthand for direction and wrap' },
      { property: 'justify-content', description: 'distributes items along the main axis' },
      { property: 'align-items', description: 'aligns items on the cross axis' },
      { property: 'gap', description: 'creates fixed spacing between items' },
    ],
    item: [
      { property: 'flex-grow', description: 'controls how much an item grows' },
      { property: 'flex-shrink', description: 'controls how much an item shrinks' },
      { property: 'flex-basis', description: 'sets the base size' },
      { property: 'align-self', description: 'changes alignment for a single item' },
      { property: 'order', description: 'changes the item order' },
    ],
  },
  summary: {
    question1: 'flex-direction',
    answer1: '1. Where do items flow?',
    question2: 'justify-content',
    answer2: '2. How do they distribute?',
    question3: 'align-items',
    answer3: '3. How do they align in the other direction?',
    note: 'Flexbox doesn\'t require much memorization; try the values, see the result, and you\'ll understand it faster.',
  },
};
