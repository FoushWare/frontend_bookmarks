import React from 'react'
import TopicPage from '../../components/TopicPage'

function Semantics() {
  return (
    <TopicPage
      icon="🏗️"
      title="Semantics"
      description="Master semantic HTML elements and proper document structure"
      backLink="/html"
      backLabel="Back to HTML"
    >
      <p className="text-gray-600">Semantics content will be migrated from the archive.</p>
    </TopicPage>
  )
}

export default Semantics
