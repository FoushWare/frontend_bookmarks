---
import Layout from '../../../layouts/Layout.astro';
import ProjectWorkspace from '../../../components/ProjectWorkspace';
import { gridProjects } from '../../../data/grid-projects';
import type { Project } from '../../../types/project';

export function getStaticPaths() {
  return gridProjects.map((project) => ({
    params: { id: project.id },
  }));
}

const { id } = Astro.params;
const project = gridProjects.find(p => p.id === id);

if (!project) {
  return Astro.redirect('/frontend_bookmarks/projects/grid');
}
---

<Layout title={`${project.title} - Grid Project`}>
  <div class="project-page">
    <ProjectWorkspace client:load project={project} />
  </div>
</Layout>

<style>
  .project-page {
    max-width: 1400px;
    margin: 0 auto;
    padding: 40px 20px;
  }
</style>
